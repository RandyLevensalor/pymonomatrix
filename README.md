# pymonomatrix

Python Application to manage the Monoprice Blackbird 8x8 matrix using the restful interface

## Configuration

Device names for inputs, video outputs, and audio outputs can be configured via a `config.yaml` file in the root directory.
By default, the application looks for `config.yaml`. You can override this location using the `PYMONOMATRIX_CONFIG` environment variable.
